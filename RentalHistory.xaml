<Window x:Class="LS.RentalHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LS"
        mc:Ignorable="d"
        Title="RentalHistory" Height="450" Width="800">
    <DockPanel Margin="15">

        <Border DockPanel.Dock="Top" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <Label Content="Chọn độc giả để xem lịch sử:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <ComboBox x:Name="UserComboBox" Width="300" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Danh sách đơn hàng" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <DataGrid x:Name="OrdersGrid" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              IsReadOnly="True"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID Đơn hàng" Binding="{Binding id}" Width="*"/>
                            <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding start_date, StringFormat='dd/MM/yyyy HH:mm'}" Width="2*"/>
                            <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding end_date, StringFormat='dd/MM/yyyy HH:mm'}" Width="2*"/>
                            <DataGridTextColumn Header="Tổng số sách" Binding="{Binding total_book}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1" Padding="10" CornerRadius="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Chi tiết sách trong đơn hàng" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <DataGrid x:Name="OrderDetailsGrid" 
                               AutoGenerateColumns="False" 
                               CanUserAddRows="False" 
                               IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên sách" Binding="{Binding Book.name}" Width="2*"/>
                            <DataGridTextColumn Header="Tác giả" Binding="{Binding Book.author}" Width="2*"/>
                            <DataGridTextColumn Header="Số lượng" Binding="{Binding quantity}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

        </Grid>
    </DockPanel>
</Window>
